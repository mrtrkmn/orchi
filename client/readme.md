# Client 
The command line interface for interacting with the orchi platform.
Run `hkn -h` (using the binary) or `go run client/main.go -h` to print the possible commands.

See [the project wiki](https://github.com/mrtrkmn/orchi/wiki/CLI-specification) for the CLI specification.
